[Unit]
# sshd can bind before the network (or tailscaled) is ready; a rapid crash
# loop then trips systemd's start limit and sshd stays dead. Never give up.
StartLimitIntervalSec=0

[Service]
Restart=always
RestartSec=10
